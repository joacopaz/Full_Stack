/**
 * Methods
 */

// We have:
// length() returns size,
// .equals() returns comparison of String, compareTo() compares Unicode values
// .concat(), String1.concat(String2) will concat them
// .indexOf() finds the first ocurrance's index
// .charAt(index) returns the char at that index


public class Methods {

    public static void main(String[] args) {
        String str = "Hello World!";
        System.out.println(str.substring(2, 6));
    }
}